/**
 * Entry point for the unit suite: `npm run test:unit`.
 *
 * Runs under ts-node with node's built-in test runner, so importing a test
 * file is enough to register its cases. Add new suites here.
 */
import './onbaseExport.test';
import './courseSupervisor.test';
import './semesterFilter.test';
import './fetchRunStatus.test';
import './scheduleOfClasses.test';
